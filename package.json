{
  "name": "jobfill",
  "version": "1.0.0",
  "private": true,
  "description": "JobFill Chrome extension — no build step needed to run it; these are dev-only regression tests for the detection/autofill pipeline and the popup/options UI.",
  "scripts": {
    "test": "node tests/logic.test.js && node tests/ui-smoke.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
