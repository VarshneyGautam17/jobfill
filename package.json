{
  "name": "jobfill",
  "version": "1.0.0",
  "private": true,
  "description": "JobFill Chrome extension — no build step needed to run it.",
  "scripts": {
    "validate": "node scripts/validate-manifest.js",
    "package": "node scripts/package-extension.js"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
